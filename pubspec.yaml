name: wearwise
description: "A new Flutter project."

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.2
  firebase_storage: ^12.4.1
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  cupertino_icons: ^1.0.8
  http: ^0.13.6  # Keep this version of http
  googleapis: ^11.0.0
  googleapis_auth: ^1.4.1  # Downgrade googleapis_auth
  geolocator: ^9.0.1
  intl: ^0.18.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/wearwise.png"

flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/wearwise.png
    - assets/credentials.json

